#+title: Majutsu - Emacs Interface for Jujutsu

Majutsu provides a Magit-style interface for [[https://github.com/jj-vcs/jj][Jujutsu (jj)]], offering an
efficient way to interact with JJ repositories from within Emacs.

[[file:docs/majutsu.org][Read the full manual]] for comprehensive documentation.

* Quick Start

** Installation

#+begin_src emacs-lisp
;; Doom Emacs (packages.el)
(package! majutsu :recipe (:host github :repo "0WD0/majutsu"))

;; use-package + straight.el
(use-package majutsu
  :straight (:host github :repo "0WD0/majutsu"))

;; use-package + package-vc (Emacs 29+)
(use-package majutsu
  :vc (:url "https://github.com/0WD0/majutsu"))
#+end_src

** Usage

1. Open a JJ repository: ~M-x majutsu~ (or ~majutsu-log~)
2. Navigate: ~n~ / ~p~ to move between revisions, ~[~ / ~]~ to jump to visible parents/children
3. Act: ~RET~ to visit, ~?~ for help
4. In blob buffers, press ~e~ to enter editable mode; save applies via =jj diffedit= (~i~ in Evil also enters editable mode)
5. In blob buffers, press ~b~ to annotate; ~C-c m~ opens the blob in Magit

** Essential Keybindings (Emacs / Evil)

| Key       | Action               |
|-----------+----------------------|
| ~n~ / ~j~     | Next revision        |
| ~p~ / ~k~     | Previous revision    |
| ~[~         | Go to parent         |
| ~]~         | Go to child          |
| ~RET~       | Visit thing at point |
| ~?~         | Show all commands    |
| ~g~ / ~g r~   | Refresh              |
| ~a~         | Absorb               |
| ~b~         | Bookmarks            |
| ~c~         | Describe             |
| ~C~         | Commit               |
| ~d~         | Diff                 |
| ~E~         | Ediff                |
| ~e~         | Edit change          |
| ~G~ / none  | Git                  |
| ~k~         | Abandon              |
| ~o~         | New                  |
| ~r~         | Rebase               |
| ~V~ / ~_~     | Revert               |
| ~R~         | Restore              |
| ~s~         | Squash               |
| ~S~         | Split                |
| ~y~         | Duplicate            |
| ~Z~ / ~*~     | Workspaces           |
| ~C-/~ / ~u~   | Undo                 |
| ~C-?~ / ~C-r~ | Redo                 |

* Requirements

- Emacs 29.1+
- [[https://github.com/jj-vcs/jj][Jujutsu (jj)]] v0.39.0+
- [[https://magit.vc/][magit]], [[https://github.com/magit/transient][transient]], [[https://github.com/magit/with-editor][with-editor]]

* Documentation

- [[file:docs/majutsu.org][User Manual]] - Complete guide to all features, including the template DSL's
  helper-local ~:bind-self~, compile-entry root ~self~ contexts for bare
  keyword sugar, explicit ~[:self]~ / ~[:self N]~ references into the current
  or outer implicit receiver stack, local owner-bound method/keyword lowering
  via bodyful ~majutsu-template-defmethod~ / ~majutsu-template-defkeyword~,
  syntax-level ~majutsu-template-defspecial~ lowering,
  anonymous ~:lambda~ / ~:|arg|~ values, basic semantic typing for
  helper arguments/method chains (including normalized container refs such as
  ~(:list T)~ / ~(:option T)~, specialized list methods on concrete element
  types, option-contained method chaining, string-like method dispatch and
  String conversion for upstream ~RefSymbol~ values, more accurate builtin
  helper metadata such as upstream-style conditional ~if()~ handling (the
  result is treated as conditional ~Any~ and checked at the consumption site)
  and ~label()/git_web_url()~ accepting ~Stringify~, literal-aware checks for
  upstream ~StringLiteral~ parameters,
  metadata-driven higher-order typing, the
  distinction between broad ~Any~ expression placeholders and narrower
  printable ~Template~ capability, and untyped ~:raw~ snippets defaulting to
  ~Unknown~ unless explicitly annotated), Dash-style higher-order helpers
  built on native lambda support, deferred lambda-receiver keyword
  specialization from typed calls/containers without reviving general unknown
  self fallback, more precise builtin helper signatures for common upstream
  functions, and legacy binder-based forms such as ~:map~ / ~:filter~ /
  ~:any~ / ~:all~ / ~:map-join~ as syntax-only compatibility sugar, plus the
  log column schema's ~heading/tail/body/metadata~ module model for
  anchor-line and foldable log layout, including searchable real-text tail
  fields with heading-aware copy filtering in log buffers, explicit log copy
  commands for section values / fields / modules (available through the
  dispatcher or via ~M-x~), and span properties that distinguish content from
  log decorations.
- [[file:NEWS.org][NEWS]] - Version history and changelog
- [[file:THIRD_PARTY_NOTICES.org][Third-Party Notices]] - Upstream adaptation and attribution map
- [[file:LICENSE-MIT][Legacy MIT Notice]] - Retained MIT attribution text for pre-GPL code

* License

- Current: [[file:LICENSE][GNU GPL v3 or later]]
- Historical notice: [[file:LICENSE-MIT][MIT notice text]] retained for pre-GPL distributions

* Credits

Majutsu began as a fork of [[https://github.com/bolivier/jj-mode.el][jj-mode.el]] by Brandon Olivier. It depends on
and draws inspiration from [[https://magit.vc/][Magit]] and wraps the [[https://github.com/jj-vcs/jj][Jujutsu]] VCS.

* Contributing

Issues and pull requests welcome at https://github.com/0WD0/majutsu
