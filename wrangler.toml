name = "majutsu-docs"
pages_build_output_dir = "dist"
compatibility_date = "2026-03-29"

# Cloudflare Pages receives the already-validated development preview through
# Direct Upload. Production publishing will be designed with the v0.7 release.
